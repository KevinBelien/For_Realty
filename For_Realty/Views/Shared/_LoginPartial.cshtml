@using Microsoft.AspNetCore.Identity
@using For_Realty.Areas.Identity.Data
@inject SignInManager<AccountUser> SignInManager
@inject UserManager<AccountUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        @*<li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Account</a>
        </li>*@
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Ad" asp-action="Create" >Zoekertje plaatsen</a>
        </li>
        @*<li class="nav-item">
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>*@
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle navigationButtons text-dark" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Account
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                <a class="form-inline btn btn-link navbar-btn navbar-link dropdown-item py-2" asp-area="Identity" asp-page="/Account/Manage/Index">Mijn profiel</a>
                <a class="form-inline btn btn-link navbar-btn navbar-link dropdown-item py-2" asp-controller="Favorite" asp-action="Index">Mijn verlanglijstje</a>
                <a class="form-inline btn btn-link navbar-btn navbar-link dropdown-item py-2" asp-controller="Ad" asp-action="Index">Mijn zoekertjes</a>
                <hr class="py-0 my-1"/>
                <form class="form-inline btn btn-link navbar-btn navbar-link dropdown-item my-0 py-0 px-3" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-link py-2 text-dark emp-nav-button">Afmelden</button>
                </form>
            </div>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
        </li>
    }
</ul>
